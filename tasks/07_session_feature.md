# Session Feature
1. Initialize `skills/session_timer.md`.
2. Format `index.html` structure supporting new `#session-timer-display` and `#session-toggle-btn`.
3. Construct supporting styling in `style.css` keeping app minimal footprint un-violated.
4. Create new functional component file `sessionTimer.js`.
5. Link component within root logic load chain (`app.js` and `index.html`) to invoke listeners permanently globally.
6. Flush logs correctly to `agent_memory/build_logs.md`.
